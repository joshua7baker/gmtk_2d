using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemySelectionModel : MonoBehaviour
{
    public enum ObstacleTypes { hostileRanged, obstacleBoulder };
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
